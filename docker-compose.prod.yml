services:
  redis:
    image: redis:7
    restart: unless-stopped
    ports:
      - "6379:6379"

  minio:
    image: minio/minio:latest
    restart: unless-stopped
    command: server /data --console-address ":9001"
    environment:
      MINIO_ROOT_USER: ${MINIO_ROOT_USER}
      MINIO_ROOT_PASSWORD: ${MINIO_ROOT_PASSWORD}
    volumes:
      - minio_data:/data
    ports:
      - "9002:9000"
      - "9001:9001"
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:9000/minio/health/ready"]
      interval: 5s
      timeout: 3s
      retries: 10

  minio-init:
    image: minio/mc:latest
    depends_on:
      minio:
        condition: service_healthy
    entrypoint: >
      /bin/sh -c "
      until mc alias set local http://minio:9000 ${MINIO_ROOT_USER} ${MINIO_ROOT_PASSWORD}; do echo 'waiting for minio...'; sleep 2; done;
      mc mb -p local/${S3_BUCKET} || true;
      exit 0;
      "

  api:
    build:
      context: .
      dockerfile: docker/Dockerfile.api
    restart: unless-stopped
    env_file: .env
    environment:
      ROLE: api
      PORT: "8081"
      REDIS_URL: redis://redis:6379
      RUN_ATTEMPTS: ${RUN_ATTEMPTS:-2}
      RUN_BACKOFF_MS: ${RUN_BACKOFF_MS:-3000}
      RUN_STALE_TIMEOUT_MINUTES: ${RUN_STALE_TIMEOUT_MINUTES:-30}
    depends_on:
      - redis
      - minio
      - minio-init
    volumes:
      - api_data:/data
    ports:
      - "8082:8081"

  worker:
    build:
      context: .
      dockerfile: docker/Dockerfile.worker
    restart: unless-stopped
    env_file: .env
    environment:
      ROLE: worker
      REDIS_URL: redis://redis:6379
      API_BASE_URL: http://api:8081
      WORKER_CONCURRENCY: "1"
    depends_on:
      - api
      - redis
      - minio
    shm_size: "2gb"
    ipc: host
    init: true

  web:
    build:
      context: .
      dockerfile: docker/Dockerfile.web
      args:
        NEXT_PUBLIC_API_BASE: ${NEXT_PUBLIC_API_BASE}
    restart: unless-stopped
    env_file: .env
    environment:
      ROLE: web
      NEXT_PUBLIC_API_BASE: ${NEXT_PUBLIC_API_BASE}
    depends_on:
      - api
    ports:
      - "3006:3000"

volumes:
  api_data:
  minio_data:
